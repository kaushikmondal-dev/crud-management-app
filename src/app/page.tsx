import { Metadata } from "next";

export const metadata: Metadata = {
  title: "User Details | CRUD App",
  description: "User Details Card of CRUD App",
};

const page = () => {
  return <section></section>;
};

export default page;
